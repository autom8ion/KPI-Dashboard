# QA KPI Report — 2026-09-03

> **Sample report.** Generated by running the `qa-kpi-report` skill's own queries directly
> against `make demo`'s synthetic seed data (no live GitHub/DevLake connection) — included so
> the shape of a real report is visible without needing credentials wired up first. A
> live-generated report replaces this file the same way, same naming (`reports/<YYYY-MM-DD>.md`).

## Headline

Overall pass rate slipped from 94.3% to 86.7% week-over-week, driven almost entirely by
`backend-agentic`: its GraphQL suite's `test_query_order` started failing on 2026-08-28 and
hasn't recovered, dragging that repo's CI success rate to 0% for the week (down from 57.1%).
`playwright-agentic` also softened (CI success 71.4% → 50.0%) but stayed above a 90% pass rate;
`k6-agentic` held steady at 100%.

## DORA

Not available — DevLake wasn't reachable when this report was generated (expected for the
local demo without `docker compose up`; see `ARCHITECTURE.md`).

## Notable regressions

- **`backend-agentic` / `tests/graphql/test_orders_graphql.py::test_query_order`** — failing
  since 2026-08-28, 6 failures this week, 0% pass rate this window. The single biggest driver
  of the repo's pass-rate and CI-success drop.
- **`backend-agentic` / `tests/e2e/test_order_lifecycle.py::test_full_order_lifecycle`** — 2
  failures this week; likely downstream of the same GraphQL regression rather than an
  independent issue, since the e2e suite exercises the GraphQL path.
- **`backend-agentic` / `tests/db/test_orders_db.py::test_created_order_is_persisted`** — 1
  failure, first seen 2026-08-31 — worth watching, not yet a trend.

## Flaky tests

Two stand out with a high flip rate over their last 10 runs on `main`:
- **`playwright-agentic` / `bookstore.spec.ts::test searches for a book @regression`** — 6 flips.
- **`backend-agentic` / `test_order_lifecycle.py::test_full_order_lifecycle`** — 6 flips (also
  failing outright this week — see above).

Five more are flagged with 1–2 flips each (`login.spec.ts`, `test_orders_rest.py`,
`test_orders_db.py`, `test_query_order`) — lower priority than the two above.

## Recommended actions

1. Quarantine `test_query_order` (`backend-agentic`) and investigate the GraphQL
   schema/resolver change around 2026-08-28 — it's the single highest-leverage fix, responsible
   for most of this week's regression.
2. Check whether `test_full_order_lifecycle`'s failures are a direct consequence of the
   GraphQL regression before treating it as a separate flaky-test issue.
3. Stabilize `bookstore.spec.ts`'s "searches for a book" test (6/10 flips) — investigate for a
   race condition or unstable fixture before it erodes trust in the `playwright-agentic` suite.
4. Re-check `playwright-agentic`'s CI success rate (71.4% → 50.0%) next week — currently below
   its pass-rate trend, which suggests some run-level (not test-level) failures worth a
   separate look.
